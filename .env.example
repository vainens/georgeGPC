# Never commit .env. This key stays on the server, never in browser code.
OPENAI_API_KEY=replace_with_your_provider_key
# Optional: OpenAI-compatible endpoint and model aliases.
# AI_BASE_URL=https://api.openai.com/v1
# GPC_LITE_MODEL=gpt-4.1-mini
# GPC_FLASH_MODEL=gpt-4.1-mini
# GPC_PREVIEW_MODEL=gpt-4.1
